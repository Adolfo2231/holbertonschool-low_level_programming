#include <stdio.h>

/**
 * Print all single-digit numbers from 0 to 9
 * with loop while
 *
*/

int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%d", n);
		n++;
	}
	printf("\n");
	return (0);
}
