#include <stdio.h>
/**
 * main - Entry point of the program
 *
 * Description: This program prints a statement about art,
 * demonstrating the beauty and depth of artistic expression.
 *
 * Return: Always returns 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
